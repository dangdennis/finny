package controllers

import "testing"

func TestYNABControllerPostAuthorize(t *testing.T) {
	// todo(rani): write your test here
	// setup the SERVER
	// setup the real ynab controller and everything required for the server setup
	// setup a mock ynab oauth service to pass to ynab controller

	// assertions
	// 1. i get some url (ynaboauth service should return any string)
	// 2. i get a 307
}
